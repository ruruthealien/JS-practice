// Immediately Invoked Function Expressions (IIFE)
// to remove global scope pollution we use iife

(function name() {
    console.log("rups");
})();
// ()(): function then calling

(() => {
    console.log("rupanjana");
    
})();